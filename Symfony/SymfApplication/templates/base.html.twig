<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
        <style>
            body {
                background: #FEF2F2;
            }
            img {
                max-width: 100%;
                display: flex;
            }
            .titel {
                color: #B6666F;
            }
            @media only screen and (min-width: 1280px) {
                img{
                    height: 15vw;
                }
            }
            .flex {
                display: flex;
                align-items: center;
                justify-content: center;
            }
            .vertical {
                margin-top: 27vh;
            }
        </style>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        <!--<a href="{{url('genproducts')}}">Generate pdf products</a><br>
        <hr>-->
        <div class="container">
        <nav class="navbar navbar-expand-md navbar-light">
            <a href="{{url('welcome')}}" class="navbar-brand">ProductShop</a>
            <button
                    class="navbar-toggler"
                    type="button"
                    data-bs-toggle="collapse"
                    data-bs-target="#toggleMobileMenu"
                    aria-controls="toggleMobileMenu"
                    aria-expanded="false"
                    aria-label="Toggle navigation"
            >
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="toggleMobileMenu">

                <ul class="navbar-nav ms-auto text-center">
                    <li>
                        <a href="{{url('welcome')}}" class="nav-link">Home</a>
                    </li>
                    <li>
                        <a href="{{url('listproducts')}}" class="nav-link">Alle producten</a>
                    </li>
                    <li>
                        <a href="{{url('addNewProduct')}}" class="nav-link">Toevoegen</a>
                    </li>
                </ul>
            </div>
        </nav>
        </div>
        {% block body %}{% endblock %}
    </body>
</html>
